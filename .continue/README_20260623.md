# git / "Auto Git" — Estado e Pendências

> Snapshot gerado em **23/06/2026**. Em snapshots futuros, crie um novo `README_AAAAMMDD.md` nesta pasta.

## O que é

Repositório utilitário **"Auto Git"** (`git@github.com:samirhvbr/GIT.git`) — coleção de scripts shell/batch para gerenciar em lote **17 repositórios** do workspace `~/x/` (clonar via `gh`, `pull --ff-only`, `push`), com versões espelhadas Linux/macOS (`.sh`) e Windows (`.cmd`). Inclui um template de deploy para apps Laravel (Blue3/SHV).

## Estado do git

- Branch `master`, sincronizado com `origin/master` (`584e6bd`). Não há `main`.
- Working tree "limpo" para arquivos rastreados; sem commits não enviados; stash vazio.
- Último commit: `584e6bd v1.5.0 - clone via gh (Windows e Linux, sem usuario/senha)`.

## Pendências

Não há marcadores `TODO/FIXME` no código — os scripts estão completos. As pendências são de **rastreamento/sincronização git e documentação**:

### 1. Arquivos de trabalho reais NÃO versionados (ignorados pelo `.gitignore`)
O `.gitignore` ignora tudo e só libera `git_*`, `README.md`, `.gitattributes`, `.gitignore`, `.claude/`. Ficam fora do versionamento:
- `status_git.sh` (script funcional de 4,6 KB, criado em 22/jun — nunca commitado)
- `deploy/deploy.sh.template` (template de deploy completo)
- `deploy/README.md` (120 linhas de documentação do padrão de deploy)
- `.claude/json-opus`, `.claude/json-fable5-opus`, `.claude/json-fable5-opus-sonnet`, `.claude/settings.json`, `.claude/settings.local.json`

→ **Provável esquecimento**: adicionar `!deploy/` e `!status_git.sh` ao `.gitignore` (a pasta `deploy/` inteira está fora do git, mesmo o `git_clone_drive.*` irmão estando versionado).

### 2. README desatualizado vs. estrutura real
- `README.md:19-32` (seção "Estrutura") **não menciona** `git_clone_drive.sh`, `git_clone_drive.cmd` nem `status_git.sh`, que existem na raiz.
- `README.md:30-31` descreve `.claude/` só com `README.md`, mas há também `json-*` e `settings*.json`.
- `git_clone_drive` (espelho Nextcloud, citado no commit `5aa9460`) não aparece em nenhuma tabela.

## Próximos passos sugeridos
1. Decidir se `deploy/`, `status_git.sh` e os snapshots `.claude/json-*` devem ser versionados → ajustar `.gitignore` (`!deploy/`, `!status_git.sh`) e commitar.
2. Atualizar `README.md` (estrutura, scripts de drive, conteúdo real de `.claude/`).
